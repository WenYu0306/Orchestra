# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# SSE support
sse-starlette>=1.8.0

# Config & env
PyYAML>=6.0
python-dotenv>=1.0.0

# Resume parsing
PyMuPDF>=1.23.0

# HTTP client (for API calls to DeepSeek / Qwen VL)
httpx>=0.26.0

# Browser automation — nodriver (CDP direct, no WebDriver, BOSS 直聘零检测)
nodriver>=0.50

# Data validation
pydantic>=2.5.0
